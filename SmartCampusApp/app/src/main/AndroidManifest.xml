<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:name=".SmartCampusApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartCampus"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Auth Activities -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartCampus.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.RegisterActivity" />

        <!-- Main Dashboard -->
        <activity android:name=".ui.student.StudentDashboardActivity" />
        <activity android:name=".ui.student.ProfileActivity" />
        <activity android:name=".ui.student.EditProfileActivity" />

        <!-- Skills -->
        <activity android:name=".ui.student.SkillRecommendationActivity" />
        <activity android:name=".ui.student.SkillMapActivity" />
        <activity android:name=".ui.student.SkillRoadmapActivity" />

        <!-- Jobs -->
        <activity android:name=".ui.jobs.JobListActivity" />
        <activity android:name=".ui.jobs.JobDetailActivity" />
        <activity android:name=".ui.jobs.ApplicationTrackerActivity" />
        <activity android:name=".ui.jobs.ExternalJobsActivity" />


        <!-- Resume -->
        <activity android:name=".ui.resume.ResumeBuilderActivity" />
        <activity android:name=".ui.resume.AtsScoreActivity" />

        <!-- Career -->
        <activity android:name=".ui.student.CareerRoadmapActivity" />

        <!-- Officer -->
        <activity android:name=".ui.officer.OfficerDashboardActivity" />

        <!-- Recruiter -->
        <activity android:name=".ui.recruiter.RecruiterDashboardActivity" />

        <!-- Admin -->
        <activity android:name=".ui.admin.AdminDashboardActivity" />

        <!-- Notifications -->
        <activity android:name=".ui.notifications.NotificationCenterActivity" />

    </application>
</manifest>
